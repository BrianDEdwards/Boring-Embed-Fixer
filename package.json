{
  "name": "boring-embed-fixer",
  "version": "1.0.0",
  "description": "Discord bot for fixing social embeds",
  "main": "bot.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -rf ./bot && tsc --p ./tsconfig.json && node ./bot/bot.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^12.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3"
  }
}
